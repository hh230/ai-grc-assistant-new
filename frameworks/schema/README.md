# Framework definition schema

The canonical schema every framework definition under `frameworks/<id>/<version>/` must
satisfy (framework -> domains -> controls -> requirements -> evidence expectations).
See CLAUDE.md §13. Schema files added in a later, reviewed PR — data only, no code.
