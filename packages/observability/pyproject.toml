[project]
name = "grc-observability"
version = "0.0.0"
description = "Logging, tracing, metrics and LLM-call audit."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["grc_observability"]
