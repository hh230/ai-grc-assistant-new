[project]
name = "grc-services"
version = "0.0.0"
description = "Services layer: coordinates domain logic and transactions."
requires-python = ">=3.12"
dependencies = ["grc-domain"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["grc_services"]
