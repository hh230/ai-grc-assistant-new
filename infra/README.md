# Infrastructure as Code

Provisions cloud resources (network, database, vector store, queue, IAM). Distinct from
`docker/` which builds images/local envs. Scaffold only — no modules implemented yet.
