# Cross-cutting tests

Only suites that span multiple apps/packages live here: `e2e/`, `missions/` (full Mission
Lifecycle incl. human-gate & cancel paths), `eval/` (AI grounding/accuracy rubrics),
`contract/`. Package-local unit tests live inside each package. Scaffold only.
