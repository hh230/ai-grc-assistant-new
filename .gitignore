# ---- Node / Next.js ----
node_modules/
.pnpm-store/
.next/
out/
dist/
build/
*.tsbuildinfo
.turbo/

# ---- Python ----
__pycache__/
*.py[cod]
.venv/
venv/
.uv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
htmlcov/
.coverage
*.egg-info/

# ---- Env & secrets ----
.env
.env.*
!.env.example

# Local Claude Code settings — personal machine config, may contain sensitive
# command allowlist entries typed during a session. Never share/commit.
.claude/settings.local.json

# ---- OS / editor ----
.DS_Store
Thumbs.db
*.log

# ---- Build / infra ----
*.tfstate
*.tfstate.*
.terraform/

# Local document storage (Upload Center)
.data/

# ---- V2 knowledge pipeline: derived/regenerable artifacts ----
# Rebuilt by the knowledge-importer package: chunks from knowledge/imports/,
# embeddings from knowledge/chunks/ (deterministic local provider). This is a
# build cache, not source — do not version-control it. knowledge/imports/ and
# knowledge/manifests/ ARE kept (effective source text + ingestion audit trail).
v2/knowledge/chunks/
v2/knowledge/embeddings/
.vercel
.env*

# Standalone uv projects under v3/ generate their own lockfiles on first run. Unlike v2/, none
# of them are committed, so leaving these untracked means a permanently dirty working tree.
v3/packages/*/uv.lock
