[project]
name = "grc-domain"
version = "0.0.0"
description = "Domain layer (DDD bounded contexts). Pure, no framework dependencies."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["grc_domain"]
